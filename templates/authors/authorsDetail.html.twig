{% extends 'base.html.twig' %}
<div class="example-wrapper">
	<div class="title">
		<h1>{{ authorsDetail.name}}</h1>
	</div>
			<div class="container">
				<div class="text-center">
					<div class="row">
						<div class="col-lg-3">
							<div class="card">
								<img class="card-img-top" src="{{ authorsDetail.photo }}" alt="Card image cap">
								<div class="card-body">
									<h1 class="card-title"></h1>
										<a href="{{ movieDetail.link }}">{{ movieDetail.name }}</a>
											<p class="card-text"></p>
								</div>
							</div>
						</div>


{% endblock %}
